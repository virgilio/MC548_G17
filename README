Ignore
Global setup:
 Download and install Git
  git config --global user.name "Your Name"
  git config --global user.email virgilio.santos@gmail.com
        
Next steps:
  mkdir MC548_G17
  cd MC548_G17
  git init
  touch README
  git add README
  git commit -m 'first commit'
  git remote add origin git@github.com:virgilio/MC548_G17.git
  git push -u origin master
      
Existing Git Repo?
  cd existing_git_repo
  git remote add origin git@github.com:virgilio/MC548_G17.git
  git push -u origin master